{
  "name": "cla-ebooks",
  "private": true,
  "version": "0.1.2",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "tauri": "tauri",
    "build:sidecar": "pwsh -NoProfile -File scripts/build-sidecar.ps1",
    "tauri:build": "tauri build",
    "format:check": "biome format . && cargo fmt --manifest-path src-tauri/Cargo.toml -- --check && py -m ruff format --check src-tauri/sidecar",
    "lint": "biome lint . && cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings && py -m ruff check src-tauri/sidecar",
    "test:frontend": "vitest run",
    "test:rust": "cargo test --manifest-path src-tauri/Cargo.toml",
    "test:python": "py -m unittest discover -s src-tauri/sidecar/tests",
    "test": "bun run test:frontend && bun run test:rust && bun run test:python",
    "check": "bun run format:check && bun run lint && bun run typecheck && bun run test"
  },
  "dependencies": {
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.8",
    "@tauri-apps/cli": "^2",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.4",
    "vitest": "^4.1.10"
  }
}
